# Remove the inherited panel and idle blanker, refresh cached system status,
# then open the writer as the appliance shell.
/usr/local/bin/writing-environment-session-setup
/usr/local/bin/writing-environment &
